#include <bits/stdc++.h>
using namespace std;

int main(){
    //Polymorphism  - one name and multiple forms
    //              - e.g. function overloading , operator overloading 
    //               - e.g. virtual functions

  // 2 type of Polymorphism :

  // 1.)compile time polymorphism - early binding(already decided by compiler)
  // achieved via - a.) function overloading  
  //                  b.) operator overloading

  //2.)Run time polymorphism - late binding at run time
  //        - via virtual function 
    return 0;
}